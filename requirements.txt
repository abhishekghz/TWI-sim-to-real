# Python dependencies for the TWI sim-to-real project
# Install with:  pip install -r requirements.txt
# (Versions are those used to produce the results; newer minor versions are usually fine.)

numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
torch>=2.0          # CPU build is fine; GPU optional (set DEV="cuda" in run_experiments.py)
